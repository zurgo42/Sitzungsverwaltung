# MinD-Referentenliste - Apache Konfiguration (ohne Options-Direktive)

# UTF-8 Encoding
AddDefaultCharset UTF-8

# Zugriff auf versteckte Dateien verhindern (Apache 2.4 Syntax)
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

# Zugriff auf sensible Dateien verhindern
# Hinweis: config.php wird durch .gitignore geschützt (nicht im Repository)
<FilesMatch "\.(md|sql|log|ini|bak|old|example)$">
    Require all denied
</FilesMatch>

# Security Headers (nur wenn mod_headers verfügbar)
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# GZIP Kompression (nur wenn mod_deflate verfügbar)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Browser Caching (nur wenn mod_expires verfügbar)
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
</IfModule>
